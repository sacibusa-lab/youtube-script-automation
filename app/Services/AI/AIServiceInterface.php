<?php

namespace App\Services\AI;

interface AIServiceInterface
{
    // Define the interface methods if needed, or keep it empty for now as a marker
    // Based on usage, it likely needs generate or similar methods
}
